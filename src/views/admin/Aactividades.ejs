<%- include("./panelPartials/header.ejs")  %> 
     <!--inicio /#page-content-wrapper -->
        <% if(login){ %>
            <style>
                .ac-active{
                    color:var(--main-text-color);
                }
            </style>
             <div class="p-2">
            <div class="container bg-white ">
                <div class="table-responsive row justify-content-center">
                    <div class="table-wrapper">
                        <div class="table-title">
                            <div class="row pt-2">
                                <div class="col-sm-10"><h2>Actividades</b></h2></div>
                                <div class="col-sm-2">
                                   <a href="/admin/createAc"  class="btn btn-primary"><i class="far fa-plus-square"></i>  Nuevo</a>
                                </div>
                            </div>
                        </div>
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th class="col-1">id</th>
                                    <th class="col-2">Nombre</th>
                                    <th class="col-2">Descripcion</th>
                                    <th class="col-1">clase</th>
                                    <th class="col-1">post</th>
                                    <th class="col-2">fecha Inicio</th>
                                    <th class="col-2">fecha Entrega</th>
                                    <th class="col-1">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (data) { %>
                                    <% for(var i = 0; i < data.length; i++) { %>
                                      <tr>
                                        <td><%= data[i].id %></td>
                                        <td><%= data[i].name %></td>
                                        <td><%= data[i].description %></td>
                                        <td><%= data[i].class %></td>
                                        <td><%= data[i].post %></td>
                                        <td><%= data[i].init_date %></td>
                                        <td><%= data[i].deadline %></td>
                                        <td>
                                    
                                            <a class="edit"  href="/admin/updateAc/<%= data[i].id%>" title="Edit" data-toggle="tooltip"><i class="material-icons">&#xE254;</i></a>
                                            <a class="delete" href="/admin/deleteAc/<%= data[i].id%>" title="admin" data-toggle="tooltip"><i class="material-icons">&#xE872;</i></a>
                                        </td>
                                      </tr>
                                    <% } %>
                                  <% } %>
                                   
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>     
        </div>
         <% }else{ window.location="/"}%>  <!--{ver como redireccionar} -->
    <!--fin /#page-content-wrapper -->

    
<%- include("./panelPartials/footer.ejs")  %> 